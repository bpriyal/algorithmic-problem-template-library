class Solution {
    public int[][] kClosest(int[][] points, int k) {
        Random random = new Random();
        for (int i = 0 i < 10; ++i) {
            int pivotInd = random.nextInt(point.length) - 1;
            System.out.println(pivtoInd);
        }
        return null;

    }
}